name = "pharmasim-payment"
main = "creem-worker.js"
compatibility_date = "2024-12-01"

# 配置自定义域名（可选）
# routes = [
#   { pattern = "payment.pharmasim.com", custom_domain = true }
# ]

# 环境变量（CREEM_API_KEY 请用 wrangler secret 设置）
[vars]
ENVIRONMENT = "production"
